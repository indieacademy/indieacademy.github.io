box: debian
build:
    steps:
        - arjen/hugo-build:
            version: "0.19"
deploy:
    steps:
        - install-packages:
            packages: git ssh-client
        - lukevivier/gh-pages@0.2.1:
            token: $GIT_TOKEN
            domain: www.indie.academy
            basedir: public